# Docker Compose for IBM DB2

<https://www.ibm.com/docs/en/db2/12.1.0?topic=system-linux>

```sh
cp .env.template .env
cp compose.yaml.template compose.yaml

docker compose up -d
```

最新のタグ: `11.5.9.0` <https://hub.docker.com/r/ibmcom/db2>
